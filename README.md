// C PROGRAMS to find the size of processor using pointer.
#include <stdio.h>
int main()
{
    int *ptr;
    printf("size of the processor's word(pointer size):%zu bytes \n",sizeof(ptr));
    return 0;
}
